describe("Problem5", function() {
    it("should return ways to make change of input value given coin denominations", function() {
        console.log('Currently Problem 5 is not solved');
        expect(change.makeChange(4, [1, 2, 3])).toEqual(4);
    });
});
